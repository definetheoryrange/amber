# TechLink Navigator

TechLink Navigator 是一个面向开发者、技术研究人员与开源生态参与者的高质量技术资源导航与外链汇总系统。该项目定位于解决技术信息过载背景下的资源筛选与结构化组织问题，帮助用户快速定位到真实可用、领域垂直、持续维护的技术站点与工具集合。项目不生产内容，而是通过人工审核与社区贡献机制，构建一个高信噪比的技术入口层。

目标用户包括：正在学习特定技术栈的初中级开发者、需要查找最新影视转码工具链的多媒体技术爱好者、以及希望跟踪动漫渲染与字幕组协作流程的开源贡献者。项目通过目录化分类、可用性检测与标签体系，将分散的垂直领域站点转化为可检索、可追溯、可反馈的资源图谱。

## 功能概览

- **垂直领域分类索引** 按照影视技术、动漫资源、剧集追踪、综合工具等维度建立一级分类，每个分类下支持二级标签过滤，便于用户按需浏览。

- **链接可用性健康检查** 系统定期对收录的每一个外链执行 HTTP 状态码检测与响应超时监控，并在前端标注最近检查时间与状态标记（正常/异常/待复查）。

- **社区贡献与投票机制** 注册用户可提交新的资源链接，经至少两名维护者审核通过后上线；同时用户可对已有链接进行“有用/失效”投票，投票数据影响排序权重。

- **批量导入与格式校验** 支持通过 CSV 或 JSON 文件批量导入链接数据，系统自动校验 URL 格式合法性、去重检测，并生成导入日志报告。

- **个性化收藏与标签备注** 用户可将常用链接加入个人收藏夹，并自定义标签备注（如“需代理访问”“仅限内网”），便于团队内部分享上下文。

- **RSS 订阅与变更通知** 每个分类目录提供独立的 RSS 订阅源，当该分类下新增链接或链接状态发生变更时，订阅者将收到摘要推送。

- **黑暗模式与阅读视图** 前端界面适配亮色/暗色主题，并为每个链接详情页提供无干扰的阅读视图，专注于描述文本与技术注释。

## 应用场景

- **多媒体处理工具链快速检索** 视频编码工程师或字幕组技术人员需要频繁查找最新的格式转换工具、帧率检测脚本或字幕时间轴调整服务。TechLink Navigator 的影视技术分类聚合了相关领域的常用入口，减少重复搜索成本。

- **开源项目的依赖文档溯源** 开发者在新项目启动阶段需要参考同类项目的技术选型方案或第三方服务商列表。通过本项目的资源图谱，可以快速发现相关领域的官方文档、社区论坛与 API 测试工具。

- **技术社区的内容共建与审核** 开源社区维护者可以利用本项目的贡献机制，将团队内部积累的“常用链接清单”对外公开，并借助投票数据识别已失效或过时的资源，保持团队知识库的鲜活度。

- **离线环境下的资源准备** 对于需要在隔离网络环境中进行开发的团队，可使用本项目的批量导出功能生成链接清单（含域名与简介），结合内部下载工具提前缓存所需资源。

- **技术写作的素材来源管理** 技术博主或文档撰写者在准备教程时，需要引用多个外部工具或数据源。本项目的收藏夹与备注功能可帮助整理引用列表，并快速检查链接是否仍然有效。

## 快速开始

以下操作指南适用于首次部署 TechLink Navigator 本地开发环境或镜像实例。

```bash
# 1. 克隆项目仓库
git clone https://github.com/techlink-navigator/navigator-core.git
cd navigator-core

# 2. 安装依赖（使用 Node.js 20 LTS 与 pnpm）
pnpm install

# 3. 配置环境变量（复制示例配置文件）
cp .env.example .env.local
# 编辑 .env.local 填写数据库连接字符串与管理员邮箱

# 4. 初始化数据库表结构与基础分类数据
pnpm run db:migrate
pnpm run db:seed

# 5. 启动开发服务器（默认监听 127.0.0.1:3000）
pnpm run dev
```

访问本地地址后，使用默认管理员账户（admin@techlink.local / changeme）登录，并立即修改密码。生产环境部署请参考 `docs/deployment.md`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 20.11.x LTS 或更高 | 运行时环境，推荐使用 nvm 管理版本 |
| pnpm | 8.15.x 或更高 | 包管理器，用于依赖安装与工作区管理 |
| PostgreSQL | 15.x 或更高 | 主数据库，存储链接、用户、分类及投票数据 |
| Redis | 7.x 或更高 | 缓存层，用于会话存储与链接状态检查队列 |
| MinIO / S3 兼容存储 | 2024 年及之后版本 | 用于存储用户上传的批量导入文件与日志附件 |
| Nginx | 1.24.x 或更高 | 生产环境反向代理，处理静态资源缓存与负载均衡 |
| PM2 | 5.x 或更高 | 进程守护工具，用于生产环境下的服务持久运行 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | `/docs/user-guide/` | 如何注册账号、收藏链接、提交新资源、查看健康检查报告 |
| 管理指南 | `/docs/admin-guide/` | 如何审核用户提交、管理分类标签、调整投票权重参数 |
| 开发贡献 | `/docs/contributing/` | 如何搭建开发环境、代码规范、提交 PR 流程、测试策略 |
| 架构设计 | `/docs/architecture/` | 系统模块划分、数据流图、缓存策略、扩展性设计说明 |
| API 参考 | `/docs/api/` | RESTful 接口列表、请求/响应示例、认证方式与限流规则 |
| 运维手册 | `/docs/ops/` | 日志分析、备份恢复、性能调优、故障排查 checklist |

## 资源列表

本项目的核心价值在于对以下技术及多媒体相关站点的结构化收录与持续监测。所有链接均按原始来源逐条列出，未做任何形式改写。

**影视与动漫资源分类**

- <code>dianyingtiantangzaixianbofang.org.cn</code>
- <code>mianfeidongmandaquan.org.cn</code>
- <code>dongmanzaixianguankanquanji.org.cn</code>

**剧集与追剧工具分类**

- <code>zuixindianshijuzaixianguankan.org.cn</code>
- <code>mianfeizhuijuwangzhan.org.cn</code>

**综合与专题资源分类**

- <code>dongjingdaoyibenre.org.cn</code>
- <code>tiantiankantiantianshuang.org.cn</code>

## 项目结构

```
navigator-core/
├── apps/
│   ├── web/                         # 主前端应用 (Next.js App Router)
│   │   ├── app/                     # 页面路由与布局
│   │   ├── components/              # 可复用 UI 组件 (分类卡片、链接列表、状态徽标)
│   │   └── styles/                  # 全局样式与主题变量
│   └── api/                         # 后端服务 (Fastify + tRPC)
│       ├── routes/                  # 接口路由定义 (链接 CRUD、投票、用户认证)
│       ├── services/                # 业务逻辑层 (健康检查调度、导入解析、通知服务)
│       └── workers/                 # 后台任务 (链接状态轮询、缓存刷新)
├── packages/
│   ├── db/                          # 数据库模型 (Prisma schema) 与迁移脚本
│   ├── config/                      # 共享配置 (环境变量解析、分类默认映射)
│   ├── types/                       # 跨应用 TypeScript 类型定义 (链接、用户、分类)
│   └── utils/                       # 通用工具函数 (URL 规范化、日期格式化、去重算法)
├── docs/                            # 完整文档，涵盖用户手册、管理指南、API 参考
├── tests/
│   ├── unit/                        # 单元测试 (Jest) 覆盖工具函数与业务逻辑
│   └── e2e/                         # 端到端测试 (Playwright) 覆盖关键用户路径
├── scripts/
│   ├── seed.js                      # 初始化分类与演示数据
│   ├── health-check.js              # 手动触发全量链接健康检查
│   └── export-csv.js                # 按分类导出链接清单为 CSV
├── docker-compose.yml               # 开发环境容器编排 (PostgreSQL + Redis + MinIO)
├── Dockerfile                       # 生产环境镜像构建定义
├── .env.example                     # 环境变量模板 (含数据库、缓存、存储配置)
├── package.json                     # 根包管理，定义工作区与脚本命令
└── README.md                        # 本文件，项目入口说明
```

## 贡献指南

开源社区的持续参与是本项目保持活力的基础。我们欢迎各类贡献，包括但不限于新增资源链接、修复页面展示问题、完善文档、优化健康检查算法。

1. **浏览现有议题与项目看板** 访问 GitHub Issues 页面，查找带有 `help wanted` 或 `good first issue` 标签的任务。如计划提交新资源，请先查阅分类规范，确保链接不与现有条目重复且域名解析正常。

2. **派生仓库并创建功能分支** 将主仓库派生至个人账号，克隆至本地后创建新分支，分支命名请遵循 `feat/简短描述` 或 `fix/问题编号` 格式。本地开发时请确保所有测试用例通过，并新编写覆盖新增代码的单元测试。

3. **提交代码并签署开发者原创声明** 提交信息（commit message）请使用约定式提交格式（如 `feat: 添加影视转码工具分类` 或 `docs: 更新 API 认证示例`）。每个 pull request 需附上变更摘要，并确认代码遵循项目 ESLint 与 Prettier 配置。

4. **等待至少两名维护者进行代码审查** 审查过程中可能会要求补充测试用例或调整接口设计。请及时响应反馈并推送更新。审查通过后，由维护者执行合并操作。

5. **发布后的验证与责任** 您的贡献将在下一个发布版本中上线。请关注后续用户反馈，若所提交链接出现大面积失效报告，维护者可能会联系您协助更新。

## 常见问题

**Q: 链接健康检查显示“异常”时，我是否需要手动重新提交？**

A: 系统每 72 小时自动对全部链接执行一次全面的 HTTP 检测，并记录响应状态与耗时。如果您发现某个链接状态标记为异常但实际可以正常访问，可以点击“申请复检”按钮，系统将在 1 小时内优先重新检测该链接。复检结果仍为异常时，该链接会被移至“待观察”分类，连续三次复检异常将自动下线并通知提交者。

**Q: 如何批量导入公司内部维护的链接清单？**

A: 在登录状态下进入“管理后台” -> “批量导入”页面，下载提供的 CSV 模板文件，按模板字段填写（必填项：链接地址、分类名称、简短描述；选填项：标签、备注）。上传文件后系统会执行格式校验并展示预览结果，确认无误后点击“执行导入”。单次导入上限为 500 条，超过此数量请分批操作或联系管理员调整阈值。

**Q: 我提交的新资源链接为什么一直没有被审核？**

A: 审核队列按照提交时间顺序处理，当前平均审核等待时间约为 48 小时。如果超过 72 小时仍未收到通过或驳回通知，请检查提交时填写的邮箱是否正确，或通过社区讨论区直接联系当前轮值审核员。审核驳回时系统会附带具体原因说明（如“域名解析失败”“内容与分类不符”“存在明显侵权风险”），您可根据原因修改后重新提交。

## 许可证

MIT License

版权所有 (c) 2026 TechLink Navigator 维护团队

特此授予任何获得本软件及相关文档文件（“软件”）副本的人无限制使用、复制、修改、合并、出版、分发、再授权和/或销售软件副本的权利，但须满足以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或主要部分中。

本软件按“原样”提供，不作任何明示或暗示的保证，包括但不限于适销性、特定用途适用性和非侵权性的保证。在任何情况下，作者或版权持有人均不对因软件或使用或其他处理软件而导致的任何索赔、损害或其他责任负责，无论是基于合同、侵权或其他理由。

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
