# TechLink Navigator

TechLink Navigator 是一个面向开发者与技术研究人员的轻量级外链资源聚合与导航系统。该项目定位于解决技术信息分散、优质资源难以追溯的痛点，通过结构化分类与简洁的索引机制，帮助用户在复杂网络环境中快速定位高价值技术站点、官方文档与实时数据源。项目本身不存储任何第三方内容，仅作为链接关系的逻辑映射层，适用于个人知识管理、团队技术栈梳理以及公开信息采集等场景。

目标用户包括独立开发者、运维工程师、技术调研人员以及开源项目维护者。项目通过纯静态页面与轻量路由规则实现快速访问，无需数据库依赖，部署成本极低，同时支持自定义分类标签与链接状态检测，可作为技术团队内部导航页的基础框架。

## 功能概览

- **多级分类索引**：支持按技术领域、服务类型、地域属性等维度对链接进行分组，每个分组可独立配置展示样式与排序权重。
- **链接健康检查**：内置异步 HTTP 状态检测机制，可定时验证每个外链的可达性，并在管理界面标记异常状态。
- **全文模糊检索**：基于标题、描述与分类标签的轻量级搜索引擎，支持拼音首字母快速匹配，提升查找效率。
- **批量导入导出**：支持 CSV 与 JSON 格式的链接批量操作，便于团队协作迁移或备份导航数据。
- **访问统计看板**：记录每个外链的点击次数与最后访问时间，辅助判断资源热度与时效性。
- **自定义主题切换**：内置明暗两套配色方案，适配不同工作环境下的视觉需求。
- **响应式栅格布局**：从桌面端到移动端自动适配，确保在各类屏幕尺寸下均具备良好的可读性与操作便捷性。

## 应用场景

- **技术团队内部导航站**：研发团队可将常用的开发文档、API 参考、CI/CD 工具链以及监控面板集中管理，新成员入职时只需访问该导航即可获得全部必要入口，大幅降低信息磨合成本。
- **开源项目依赖资源索引**：开源项目维护者可将项目所依赖的第三方库、数据源、官方公告页以及社区论坛统一收录，便于贡献者快速理解项目生态全貌。
- **技术调研与竞品分析**：市场或技术调研人员可围绕特定垂直领域（如区块链节点、赛事数据、直播平台）聚合相关站点，通过分类与注释记录调研结论，形成可复用的知识资产。
- **个人书签同步与备份**：个人开发者可将分散在多个浏览器中的书签集中导出至本系统，借助标签体系实现跨设备统一访问，同时避免浏览器厂商锁定。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，确保已安装 Git 和 Node.js（版本 >= 16.x）。

```bash
# 1. 克隆项目仓库
git clone https://github.com/techlink-navigator/navigator.git
cd navigator

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev
```

启动成功后，访问控制台输出的本地地址（默认为 http://localhost:3000）即可进入导航面板。如需构建生产环境静态文件，请执行 `npm run build`，产物将输出至 `dist` 目录，可直接部署至任何静态托管服务。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | >= 16.14.0 | 运行时环境，用于构建工具链与开发服务器 |
| npm | >= 8.0.0 | 包管理器，用于安装项目依赖 |
| Git | >= 2.25.0 | 版本控制工具，用于克隆仓库及贡献管理 |
| 现代浏览器 | 最新两个版本 | 支持 ES6+ 与 CSS Grid/Flexbox 特性 |
| 网络连接 | 稳定 | 用于首次启动时下载依赖包及检测外链状态 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | /docs/user-guide.md | 如何添加分类、导入链接、设置主题以及查看访问统计？ |
| 管理员指南 | /docs/admin-guide.md | 如何进行批量操作、配置健康检查间隔以及自定义首页布局？ |
| 开发规范 | /docs/development.md | 项目代码结构、提交规范、测试流程以及如何扩展新功能？ |
| 部署说明 | /docs/deployment.md | 支持哪些托管平台（Vercel / Netlify / 自建 Nginx）？环境变量如何配置？ |
| 常见问题 | /docs/faq.md | 链接检测超时如何处理？如何迁移旧版数据？移动端侧滑菜单失效怎么办？ |

## 资源列表

以下为当前索引库中收录的全部外链资源，按业务类别分组展示。每个链接均保持用户提供的原始格式，不做任何协议补全或域名改写。

体育赛事官方信息类：
- <code>xijiaguanfang.asia</code>
- <code>xibanyajiajiliansai.asia</code>

直播与主播服务类：
- <code>tuchaozhugongbang.asia</code>
- <code>tuchaozhibogw.asia</code>

推荐与榜单类：
- <code>tuchaotuijian.asia</code>

射手与统计数据类：
- <code>tuchaosheshoubang.asia</code>

赛程与赛果类：
- <code>tuchaosaicheng.asia</code>

## 项目结构

```
navigator/
├── src/                           # 核心源代码目录
│   ├── assets/                    # 静态资源（图片、字体、全局样式）
│   │   ├── icons/                 # SVG 图标库
│   │   └── themes/                # 明暗主题变量文件
│   ├── components/                # 可复用 UI 组件
│   │   ├── LinkCard.vue           # 外链卡片渲染组件
│   │   ├── CategoryGroup.vue      # 分类分组容器组件
│   │   ├── SearchBar.vue          # 搜索输入与结果下拉
│   │   └── HealthBadge.vue        # 链接状态指示标记
│   ├── composables/               # 组合式逻辑钩子
│   │   ├── useLinkChecker.ts      # 链接健康检测逻辑
│   │   └── useStorage.ts          # 本地数据持久化处理
│   ├── data/                      # 链接与分类的静态数据源
│   │   ├── links.json             # 主链接索引表（含分类ID与元数据）
│   │   └── categories.json        # 分类定义与排序规则
│   ├── layouts/                   # 页面布局模板
│   │   ├── default.vue            # 默认左右分栏布局
│   │   └── compact.vue            # 紧凑单栏移动布局
│   ├── pages/                     # 路由页面
│   │   ├── index.vue              # 导航首页
│   │   ├── about.vue              # 项目说明页
│   │   └── settings.vue           # 用户配置面板
│   ├── router/                    # 前端路由配置
│   │   └── index.ts               # 路由表与守卫逻辑
│   ├── store/                     # 全局状态管理
│   │   ├── linkStore.ts           # 链接数据的增删改查
│   │   └── uiStore.ts             # 侧栏折叠、主题切换状态
│   └── utils/                     # 工具函数
│       ├── validator.ts           # URL 格式校验与规范化辅助
│       └── fetcher.ts             # 封装 fetch 的超时重试机制
├── public/                        # 公共静态目录（直接复制至构建输出）
│   └── favicon.ico                # 站点图标
├── docs/                          # 项目文档（用户手册、开发指南）
│   ├── user-guide.md
│   ├── admin-guide.md
│   ├── development.md
│   ├── deployment.md
│   └── faq.md
├── scripts/                       # 辅助脚本
│   ├── health-check.js            # 独立运行的链接批量检测脚本
│   └── import-csv.js              # CSV 导入转换工具
├── tests/                         # 单元测试与集成测试
│   ├── unit/                      # Vitest 单元测试用例
│   └── e2e/                       # Playwright 端到端测试
├── .env.example                   # 环境变量模板（含检测超时阈值）
├── .gitignore                     # Git 忽略规则
├── index.html                     # 入口 HTML 模板
├── package.json                   # 项目依赖与脚本定义
├── tsconfig.json                  # TypeScript 编译配置
├── vite.config.ts                 # Vite 构建工具配置
└── README.md                      # 项目说明文件（本文件）
```

## 贡献指南

我们欢迎所有形式的贡献，包括但不限于新增分类建议、链接资源补充、代码缺陷修复以及文档改进。请遵循以下步骤参与项目：

1. 在 GitHub 仓库页面点击 `Fork` 创建个人复刻，并将复刻克隆至本地开发环境。
2. 创建新的功能分支，分支命名建议采用 `feature/描述` 或 `fix/问题编号` 格式，例如 `feature/add-esports-category`。
3. 在本地完成代码或文档修改后，请运行 `npm run test` 确保现有测试用例全部通过；新增功能需附带对应的单元测试。
4. 提交变更时请使用清晰且符合 Conventional Commits 规范的提交信息，例如 `feat: add link health check retry mechanism`。
5. 将分支推送至个人复刻仓库，然后通过 GitHub 界面发起 Pull Request 至主仓库的 `main` 分支，等待项目维护者审核。

## 常见问题

**Q：链接健康检查显示超时，但实际浏览器可以访问，应如何处理？**

A：部分站点可能对 `User-Agent` 或请求频率有严格限制。您可以在 `.env` 文件中调整 `CHECK_TIMEOUT` 参数（单位毫秒）延长等待时间，或修改 `src/utils/fetcher.ts` 中的请求头部模拟真实浏览器。若为境外站点网络延迟较高，建议部署在靠近目标区域的服务器节点上。

**Q：如何在不重新构建的情况下更新链接列表？**

A：项目支持外部数据源模式。您可以在 `src/data/links.json` 中配置 `remoteUrl` 字段，指向一个远程 JSON 文件地址（需支持跨域）。系统会在启动时自动拉取远程数据并合并本地缓存，实现动态更新。若仅需本地编辑，直接修改 `links.json` 后保存，开发服务器将自动热重载。

**Q：导航分类下的链接数量过多时，页面渲染变慢，如何优化？**

A：建议启用虚拟滚动列表组件。项目已内置 `VirtualList` 组件，您可以在 `CategoryGroup.vue` 中将 `useVirtual` 属性设置为 `true`，该组件会按可视区域动态渲染 DOM 节点，大幅减少首屏加载时间。同时，您也可以按热度或字母序对链接进行分页展示，避免单次渲染超过 200 条条目。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
