# NavSphere 技术资源导航站

NavSphere 是一个面向开发者与技术研究人员的轻量级垂直领域资源导航系统，专注于聚合体育数据解析、赛事信息整合与历史统计指标查询类外部工具与数据源。项目本身不存储任何赛事原始数据，不提供数据抓取或爬虫功能，仅作为可公开访问的优质外链索引目录，帮助技术团队快速定位特定领域的数据服务入口。

项目目标用户包括体育数据分析团队、量化投研人员、API 集成开发者以及需要快速接入第三方统计接口的后端工程师。通过结构化的链接分类与简洁的查询交互，NavSphere 将分散在多个独立域名下的信息入口统一收纳，降低信息检索与书签管理成本，同时为团队内部知识库提供可扩展的外链组织模板。

## 功能概览

- **分类索引面板** 按数据用途与域名后缀自动分组展示资源入口，支持按赛事分析、实时比分、历史战绩等维度筛选。

- **链接状态检测** 每日定时对收录的 URL 发起可用性探测，在管理后台标注失效链接，辅助运维人员清理或更新条目。

- **快速检索框** 支持按域名关键词或描述标签进行模糊匹配，实时过滤显示匹配的资源卡片，提升高频使用场景下的查找效率。

- **自定义标签体系** 允许项目维护者为每个 URL 附加多个自定义标签，如“API 稳定”“需海外访问”“JSON 格式输出”等，便于团队内部约定。

- **访问统计看板** 记录每个外链的点击次数与最近访问时间，帮助管理员识别高价值资源并优化首页排序权重。

- **只读镜像模式** 支持生成当前资源列表的静态只读镜像页面，用于对外公开分享或嵌入内部 Wiki，避免主管理界面暴露。

- **导入导出功能** 提供 JSON 与 CSV 格式的链接列表批量导入导出，便于与其它书签管理工具或爬虫任务清单做数据交换。

## 应用场景

- **数据团队快速初始化分析环境** 新成员加入体育数据项目时，可通过 NavSphere 一站式获取所有已审批的外部数据源入口，无需逐个询问或翻阅零散文档，将环境准备时间从数小时压缩至十分钟内。

- **API 集成测试中的源站切换** 在开发数据聚合服务时，工程师需要频繁切换不同的数据服务商进行对比测试。NavSphere 的分类索引允许一键跳转至各备用源站，减少手动输入域名或翻阅历史邮件的操作成本。

- **运维监控的外链巡视** 系统运维人员每日借助状态检测功能快速扫描所有收录域名的响应码与延迟，提前发现证书过期、DNS 解析异常或服务下线问题，避免生产环境因外部依赖不可用导致数据缺失。

## 快速开始

以下步骤适用于在 Linux 或 macOS 开发环境中部署 NavSphere 开发实例。

```bash
# 克隆项目仓库
git clone https://github.com/navsphere/navsphere-core.git
cd navsphere-core

# 安装依赖（使用 pnpm 或 npm）
npm install

# 复制环境变量模板并编辑数据库连接配置
cp .env.example .env.local

# 执行数据库迁移与初始数据填充
npm run migrate
npm run seed

# 启动开发服务器（默认监听 3000 端口）
npm run dev
```

访问 http://localhost:3000 即可看到本地运行实例，管理员后台入口为 /admin，默认账号密码见 .env.local 中的初始化配置。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，推荐使用 nvm 管理版本 |
| pnpm | 8.x 或 9.x | 包管理器，用于依赖安装与 monorepo 工作区管理 |
| PostgreSQL | 14.x 及以上 | 主数据库，存储链接条目、标签与访问日志 |
| Redis | 7.x 及以上 | 缓存会话与链接状态探测的临时结果 |
| Nginx | 1.22 及以上 | 生产环境反向代理与静态资源缓存（可选） |
| Docker Compose | 2.20 及以上 | 用于一键启动本地开发数据库与缓存容器 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 入门指南 | /docs/getting-started | 如何从零开始部署 NavSphere、初始管理员配置、首次登录操作 |
| 维护手册 | /docs/maintenance | 如何增删改查资源链接、批量导入导出、标签分类管理流程 |
| 部署参考 | /docs/deployment | 生产环境容器化部署参数、Nginx 配置模板、SSL 证书挂载方式 |
| API 文档 | /docs/api | 后端 RESTful 接口定义、认证方式、列表查询与状态探测的调用示例 |
| 自定义开发 | /docs/customization | 主题样式替换、新增索引字段、扩展状态检测协议（支持 TCP/HTTP/HTTPS） |

## 资源列表

本节点收录的体育数据类外部资源按域名类别划分如下：

**主域名组（.com.cn 后缀）**

<code>zuqiudsjifenbang.com.cn</code>

<code>zuqiudsjifenbang.cn</code>

<code>zuqiudsfenxi.org.cn</code>

<code>zuqiudsfenxi.net.cn</code>

<code>zuqiudsfenxi.com.cn</code>

<code>zuqiudsfenxi.cn</code>

**补充域名组（.net.cn 后缀）**

<code>zuqiudsbisaijieguo.net.cn</code>

## 项目结构

```
navsphere-core/
├── apps/
│   ├── web/                         # 主前端应用（Next.js App Router）
│   │   ├── app/                     # 页面路由与布局
│   │   ├── components/              # 可复用 UI 组件（卡片、搜索条、标签）
│   │   └── hooks/                   # 自定义 React Hooks（鉴权、防抖）
│   └── admin/                       # 管理后台独立入口（React + Ant Design）
│       ├── pages/                   # 后台页面（链接管理、状态监控）
│       └── services/                # 后台 API 调用封装
├── packages/
│   ├── database/                    # Prisma Schema 与迁移脚本
│   │   ├── prisma/
│   │   └── seeds/                   # 初始资源列表与标签种子数据
│   ├── core/                        # 核心业务逻辑（链接解析、标签匹配）
│   │   ├── src/
│   │   │   ├── detector/            # 状态检测模块（HTTP/TCP 探针）
│   │   │   └── parser/              # URL 规范化与去重工具
│   └── shared/                      # 类型定义与常量（域名后缀映射、分类枚举）
├── docker/                          # 容器编排配置
│   ├── docker-compose.dev.yml       # 开发环境（PostgreSQL + Redis）
│   └── docker-compose.prod.yml      # 生产环境（含 Nginx 与 SSL 自动续期）
├── scripts/                         # 运维辅助脚本
│   ├── health-check.sh              # 批量探测所有收录链接可用性
│   └── backup-db.sh                 # 定时数据库备份
├── .env.example                     # 环境变量模板
├── package.json                     # 根 package.json（workspace 定义）
└── README.md                        # 项目说明文档（当前文件）
```

## 贡献指南

1. **认领或提交 Issue** 在 GitHub Issues 中查找带有 “help wanted” 或 “resource-request” 标签的任务，或提交新资源收录申请，说明来源域名、分类建议与使用价值。

2. **创建功能分支** 从 main 分支拉取新的 feature 分支，命名遵循 `feature/resource-{date}-{description}` 格式，例如 `feature/resource-20260811-football-stats`。

3. **遵守资源收录规范** 所有新增链接必须通过可用性检测与基础安全审查（无恶意脚本、无强制下载行为）。在 `packages/database/seeds/resources.json` 中按既定 Schema 添加条目，并同步更新对应的标签索引。

4. **运行测试与迁移** 本地执行 `npm run test` 确保核心解析函数与检测模块无回归错误，执行 `npm run migrate` 检查数据库变更是否兼容当前生产 Schema。

5. **发起 Pull Request** 提交 PR 时需填写模板中的自查清单，包含链接测试截图、标签分类说明以及是否经过管理员预览环境验证。至少一名核心维护者审核通过后合并。

## 常见问题

**Q：NavSphere 是否会主动缓存或代理第三方链接的内容？**

A：不会。NavSphere 仅存储和展示第三方 URL 的文本元数据（标题、标签、分类），不发起任何内容抓取、缓存或代理转发。所有的链接点击直接由用户浏览器跳转至原始目标服务器，项目本身不承担第三方数据可用性与准确性的责任。

**Q：收录的链接检测到不可用时，系统会自动删除该条目吗？**

A：不会自动删除。状态检测功能仅标记为“异常”并记录错误码与时间戳，由管理员人工审核后决定是否移除或联系源站维护方。此设计避免因临时网络波动导致误删有效资源。

**Q：是否支持私有化部署且不依赖外网访问？**

A：支持完全私有化部署。所有前端静态资源与后台服务均可离线运行，数据库与缓存均部署在内网环境。唯一的外网依赖仅为收录链接本身的可访问性，但状态检测功能可配置为跳过或使用内网代理进行探测。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
