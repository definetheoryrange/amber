# ResourceHub

ResourceHub 是一个面向技术开发者与内容创研团队的开源外链资源聚合与导航系统。项目定位为轻量级、可自部署的技术资源收录站点，帮助用户从分散的网络素材、文档站点与多媒体源中高效提取结构化信息，形成可维护的资源目录。目标用户包括个人站长、技术写作人员、本地化内容运营者以及小型团队内部知识库管理员。ResourceHub 通过预置的解析规则与标签体系，解决跨站点资源检索困难、链接失效难以追踪、素材来源分散且缺乏统一元数据描述的问题，使外链管理从无序收藏变为有序资产。

## 功能概览

- 多源链接收割与元数据抽取：支持批量导入 URL 列表，自动识别页面标题、编码格式、最后修改时间及关键 meta 信息，生成标准化的资源描述记录。

- 自定义标签与分类树管理：允许用户创建多级分类目录，为每条外链附加多个功能标签，并支持按标签组合进行快速筛选与统计。

- 链接健康状态巡检：内置定时巡检任务，可配置检查间隔与超时阈值，对失效链接、重定向链过长、内容变更等状态进行标记并输出异常报告。

- 全文检索与高级过滤器：基于倒排索引实现标题、描述、来源域名的快速检索，同时提供按文件类型、更新时间区间、域名后缀等多维度过滤面板。

- 数据导入导出与迁移工具：支持 CSV、JSON、Markdown 表格格式的批量导入导出，便于与其他知识管理工具或静态站点生成器集成，实现资源库的可移植性。

- 用户协作与审核流：为多成员团队提供资源提交与审核机制，普通成员可提议新增或修改链接，管理员审核通过后正式入库，并保留完整的操作日志。

- 开放 API 与嵌入组件：提供 RESTful API 供外部系统调用资源数据，同时提供可嵌入的 iframe 小部件，用于在内部 Wiki 或项目文档中展示特定分类下的资源列表。

## 应用场景

- 技术文档团队维护外部参考链接库：在编写产品手册或 API 文档时，需要引用大量第三方规范、教程和工具站。ResourceHub 可作为文档项目的子模块运行，将参考链接按产品版本和功能模块分类存储，并定期检测链接可用性，避免文档中出现死链。

- 本地化内容运营者管理多语言素材源：内容本地化过程中常涉及多个语种的术语库、翻译记忆库和风格指南站点。运营人员可利用 ResourceHub 的标签体系为每个资源标记语种、领域和可信度等级，使团队在翻译审校时能快速定位权威参考。

- 独立开发者构建个人导航站点：开发者可将日常浏览积累的优质技术博客、开源组件库、在线工具等资源录入系统，通过自定义分类树生成个人起始页，并结合全文检索快速找回数月前收藏的特定框架文档。

- 小型团队内部知识沉淀与共享：非技术岗位的运营或设计人员可通过提交链接的方式参与知识库建设，管理者通过审核流把控资源质量，最终形成团队统一的外部门户，减少重复搜索成本。

## 快速开始

以下命令演示如何从 GitHub 克隆项目源码、安装依赖并启动开发服务。请确保已安装 Node.js 18.x 与 npm 9.x。

```bash
git clone https://github.com/resourcehub/resourcehub.git
cd resourcehub
npm install
npm run build
npm start
```

生产环境部署建议使用 `pm2` 或 `systemd` 进行进程守护，并配置反向代理（如 Nginx）以处理静态资源缓存与 SSL 终结。前端静态文件位于 `dist` 目录，后端服务默认监听 `3000` 端口。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x LTS 或 20.x | 运行时环境，推荐使用 nvm 管理版本 |
| npm | 9.x 或 10.x | 包管理器，用于安装项目依赖 |
| PostgreSQL | 14.x 或 15.x | 主数据库，存储资源元数据、用户与审计日志 |
| Redis | 7.x | 缓存会话与巡检任务队列，提升高频查询响应速度 |
| Nginx | 1.22+ | 生产环境推荐反向代理，处理负载均衡与静态文件缓存 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | /docs/user-guide/ | 如何注册、登录、添加资源、管理标签、执行巡检与导出数据 |
| 管理员指南 | /docs/admin-guide/ | 如何配置审核流、调整巡检频率、管理用户权限与查看操作日志 |
| API 参考 | /docs/api-reference/ | 各端点的请求参数与返回结构说明，以及鉴权方式与限流策略 |
| 部署运维 | /docs/deployment/ | 生产环境安装依赖、数据库迁移、Nginx 配置、SSL 证书更新与备份恢复 |

## 资源列表

本项目中收录的原始外链资源按内容性质归类如下。所有链接均保留用户提供时的原始格式，未做任何协议或域名前缀的增删修改。

学术与教育类参考

<code>rihanzhongchu.org.cn</code>

<code>liulianshipin.org.cn</code>

中文内容与字幕资源

<code>yirenzhongwen.org.cn</code>

<code>tingtingzhongwenzimu.org.cn</code>

多媒体与娱乐内容源

<code>daxiangjiaojiujiu.org.cn</code>

<code>jiqingsifang.org.cn</code>

<code>guochansanjizipai.org.cn</code>

## 项目结构

```
resourcehub/
├── backend/                        # 后端服务源码（Node.js + Express）
│   ├── src/
│   │   ├── controllers/            # 路由控制器，处理资源、标签、巡检等请求
│   │   ├── models/                 # PostgreSQL 数据模型定义（Sequelize ORM）
│   │   ├── services/               # 业务逻辑层，包含收割器、巡检器与索引器
│   │   ├── middlewares/            # 鉴权、日志、限流与错误处理中间件
│   │   └── utils/                  # 通用工具函数，如 URL 规范化与日期格式化
│   ├── tests/                      # 单元测试与集成测试用例
│   └── package.json                # 后端依赖声明与脚本命令
├── frontend/                       # 前端单页应用（React + TypeScript）
│   ├── src/
│   │   ├── pages/                  # 主要页面组件（资源列表、详情、仪表板、设置）
│   │   ├── components/             # 可复用 UI 组件（表格、筛选面板、标签选择器）
│   │   ├── hooks/                  # 自定义 React Hooks（数据请求、表单状态）
│   │   ├── stores/                 # Zustand 状态管理（用户会话、筛选条件、缓存）
│   │   └── styles/                 # 全局主题变量与组件样式覆写
│   ├── public/                     # 静态资源入口与 favicon
│   └── package.json                # 前端依赖及构建脚本
├── deploy/                         # 部署相关配置
│   ├── nginx/                      # Nginx 站点配置模板与 SSL 示例
│   ├── pm2/                        # PM2 进程管理配置文件（集群模式）
│   └── docker/                     # Dockerfile 与 docker-compose 开发环境编排
├── docs/                           # 完整项目文档（用户手册、API 参考、运维指南）
├── scripts/                        # 辅助脚本（数据库初始化、种子数据、迁移工具）
├── .env.example                    # 环境变量配置模板（数据库连接、JWT 密钥等）
└── README.md                       # 项目概览与快速入口（即本文档）
```

## 贡献指南

1. 复刻仓库至个人账号，并创建功能分支。分支命名建议采用 `feature/功能简述` 或 `fix/问题编号` 格式，以便追溯变更意图。

2. 本地开发环境中完成代码修改，确保新代码通过全部现有测试用例，并为新增功能或修复编写对应的单元测试。前端变更需通过 ESLint 与 Prettier 检查。

3. 提交变更时使用规范的提交信息格式，遵循 Conventional Commits 规范（如 `feat: 增加巡检结果邮件通知` 或 `fix: 修复重定向链过长时状态标记错误`），便于自动生成变更日志。

4. 向主仓库的 `main` 分支发起 Pull Request，并在描述中清晰说明变更目的、影响范围以及测试覆盖情况。PR 标题应简洁概括改动内容。

5. 等待维护者审阅。审阅过程中可能要求补充测试或调整实现细节，请在约定时间内响应反馈。合并后您的贡献将列入项目贡献者列表。

## 常见问题

Q: 巡检任务是否会对外部站点造成过大请求压力？

A: 巡检模块默认采用并发数控制与随机延迟策略，单次巡检对同一根域名的并发请求数不超过 2 个，且请求间隔随机分布于 1 至 5 秒之间。此外，系统会读取目标站点的 `robots.txt` 中的 `Crawl-delay` 指令并予以遵守，同时支持管理员自定义全局请求速率上限，确保符合外部站点的访问规范。

Q: 如何从旧版资源收藏工具（如浏览器书签、Pocket、Raindrop.io）迁移数据？

A: 项目提供命令行迁移工具，位于 `scripts/migrate.js`，支持导入 Netscape 书签 HTML 格式、Pocket 导出的 CSV 以及 Raindrop.io 的 JSON 备份。导入时自动映射常用字段（标题、URL、标签、收藏时间），并对缺失的元数据字段填充默认值。迁移完成后可在 Web 界面中进一步整理分类与标签。

Q: 资源库的数据是否可以完全离线使用？

A: 前端界面完全依赖后端 API，因此离线模式下无法进行检索或管理操作。但项目支持定期生成静态快照（命令为 `npm run snapshot`），该命令会将当前资源库导出为静态 HTML 目录，包含全部资源列表与详情页面，可作为只读备份在本地文件系统中直接浏览，无需数据库或后端服务。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:11
