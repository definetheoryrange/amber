# DeJia Tech Resource Hub

DeJia Tech Resource Hub 是一个面向体育数据聚合、实时比分解析与赛事结果归一化处理的开源技术资源导航项目。本项目服务于体育数据开发者、赛事数据分析师以及相关平台运维人员，旨在解决多源异构赛事数据采集、清洗、归一化与快速检索过程中面临的数据源分散、接口标准不统一、可用性验证困难等实际问题。

项目本身不提供具体数据采集实现，而是作为一个经过筛选与分类的技术资源索引系统，将分散在多个垂直领域的赛事数据接口、比分查询服务、结果归档平台以结构化方式呈现，并辅以可用性检测脚本与接口适配建议，帮助开发者在中大型数据项目中快速定位可靠的数据来源，减少调研与试错成本。

## 功能概览

- **数据源分类索引** 按照赛事类型、地区、数据粒度对收录的资源进行层级分类，支持按关键词与标签筛选。

- **可用性自动检测** 内置周期性 HTTP 状态检查与响应时间统计，对每个收录资源标注可用状态与平均延迟。

- **接口适配模板生成** 根据资源返回的数据结构特征，自动生成 Python/JavaScript 两种语言的简易解析代码模板。

- **变更通知订阅** 当收录资源的接口字段、域名或 SSL 证书发生变更时，通过邮件或 Webhook 发送差异报告。

- **历史数据快照对比** 定期抓取并存储资源首页或 API 响应样例，提供版本化对比视图，便于追踪数据结构演变。

- **私有化部署支持** 提供完整的 Docker Compose 编排文件，支持在内部网络环境中一键部署私有资源导航实例。

- **社区贡献工作流** 内置资源提交、审核、测试标准流程，允许社区成员新增或更新数据源信息。

- **访问统计分析看板** 提供轻量级仪表板，展示各资源链接被点击与调用的频次趋势，辅助判断资源热度。

## 应用场景

- 体育数据平台初期调研阶段，技术团队需要快速评估多个赛事比分接口的可用性、响应速度与数据完整性，通过本项目提供的可用性检测与历史快照对比功能，可在数小时内完成原本需要数周的人工调研工作。

- 运维团队负责维护多个赛事数据聚合服务，当上游数据源出现域名更换或 SSL 证书过期时，本项目的变更订阅功能可第一时间发送告警，避免数据采集任务长时间失效。

- 开源数据竞赛或黑客松活动中，参赛者需要稳定的赛事结果测试数据集，本项目的资源列表与接口适配模板可帮助参赛者在短时间内完成数据接入，将精力集中于算法与模型设计。

- 企业内部搭建体育数据中台时，需要建立统一的外部数据源管理规范，本项目提供的索引结构与检测机制可作为内部数据源治理的参考模板，降低重复建设成本。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，请确保已安装 Git、Docker 及 Docker Compose。

```bash
# 1. 克隆项目仓库
git clone https://github.com/dejia-tech/resource-hub.git
cd resource-hub

# 2. 安装依赖（自动拉取 Python 3.11 + Node.js 18 基础镜像）
docker-compose pull

# 3. 初始化数据库并导入预设资源列表
docker-compose run --rm backend python scripts/init_db.py

# 4. 启动全部服务（Web 界面 + 检测调度器 + API）
docker-compose up -d

# 5. 访问本地 Web 界面
# 浏览器打开 http://localhost:8080
```

如需开发环境本地调试（不使用容器），请参考 `docs/development.md` 中的手动安装说明。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Docker Engine | 20.10.0 及以上 | 用于容器化运行所有服务组件 |
| Docker Compose | 2.0.0 及以上 | 编排多容器启动与网络互联 |
| Python | 3.11.x | 仅开发模式需要，生产环境使用容器内解释器 |
| Node.js | 18.x LTS | 仅前端开发模式需要，用于构建静态资源 |
| PostgreSQL | 15.x | 生产数据库，开发环境使用容器内实例 |
| Redis | 7.x | 缓存与消息队列，用于检测任务调度 |
| Git | 2.25 及以上 | 克隆仓库与版本管理 |
| 可用内存 | 不低于 2 GB | 运行容器与检测任务的最低内存要求 |
| 可用磁盘 | 不低于 10 GB | 存储历史快照与日志数据 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户指南 | `docs/user-guide/` | 如何使用 Web 界面检索资源、查看可用性状态、导出适配模板 |
| 运维手册 | `docs/ops/` | 如何配置检测频率、设置变更告警、备份快照数据、迁移服务 |
| 开发者文档 | `docs/developer/` | 如何提交新的资源条目、编写检测插件、扩展 API 接口 |
| 架构设计 | `docs/architecture/` | 系统整体模块划分、数据流向、扩展性设计决策与权衡说明 |
| 测试规范 | `docs/testing/` | 单元测试、集成测试与端到端测试的编写规范与覆盖率要求 |
| 变更日志 | `CHANGELOG.md` | 每个版本的功能新增、修复与破坏性变更记录 |

## 资源列表

本项目中收录的第三方体育赛事数据资源按域名类别整理如下。所有资源均经过初始可用性验证，但实际使用前请自行确认其服务条款与数据使用限制。

### 比分与结果类资源

- <code>fajiabifen.org.cn</code>

- <code>dejiazuqiubifenwang.org.cn</code>

- <code>dejiasaichengjieguo.org.cn</code>

- <code>dejiasaicheng.net.cn</code>

- <code>dejiajishibifen.com.cn</code>

- <code>dejiajishibifen.net.cn</code>

- <code>dejiabisaijieguo.net.cn</code>

## 项目结构

```
resource-hub/
├── backend/                          # Python FastAPI 后端服务
│   ├── app/
│   │   ├── api/                      # RESTful 接口路由定义
│   │   ├── core/                     # 配置、数据库连接、安全中间件
│   │   ├── models/                   # SQLAlchemy 数据模型（资源、检测记录、快照）
│   │   ├── schemas/                  # Pydantic 请求/响应模型
│   │   ├── services/                 # 业务逻辑层（索引、检测调度、通知）
│   │   └── utils/                    # 通用工具函数（HTTP 客户端、时间处理）
│   ├── scripts/
│   │   ├── init_db.py                # 初始化数据库表与种子数据
│   │   └── run_detector.py           # 独立运行可用性检测任务
│   ├── tests/                        # 单元测试与集成测试
│   ├── requirements.txt              # Python 依赖清单
│   └── Dockerfile                    # 后端容器构建文件
├── frontend/                         # Vue 3 + TypeScript 前端
│   ├── src/
│   │   ├── components/               # 可复用 UI 组件（资源卡片、检测状态徽标）
│   │   ├── views/                    # 页面级组件（首页、分类浏览、详情、看板）
│   │   ├── stores/                   # Pinia 状态管理（资源列表、筛选条件）
│   │   ├── api/                      # 后端 API 调用封装
│   │   └── utils/                    # 前端工具函数（日期格式化、防抖）
│   ├── public/                       # 静态资源（favicon、logo）
│   ├── package.json                  # Node.js 依赖与脚本
│   └── Dockerfile                    # 前端构建与 Nginx 托管镜像
├── detector-plugins/                 # 可插拔的特定资源检测扩展
│   ├── football/                     # 足球类资源检测逻辑
│   └── basketball/                   # 篮球类资源检测逻辑
├── docker-compose.yml                # 生产环境编排文件
├── docker-compose.dev.yml            # 开发环境热重载编排文件
├── .env.example                      # 环境变量配置示例
├── docs/                             # 全部文档（用户、运维、开发、架构）
├── CHANGELOG.md                      # 版本变更历史
├── CONTRIBUTING.md                   # 贡献者指南详细版本
└── README.md                         # 项目入口文档（本文件）
```

## 贡献指南

我们欢迎所有形式的贡献，包括但不限于新增资源链接、修正已有信息、改进检测脚本、完善文档以及提交缺陷报告。请遵循以下标准流程：

1. 在 GitHub Issues 中搜索现有议题，确认无人正在处理相同内容后，新建议题描述您希望提交的资源或改进项，并标注类型（新增资源 / 功能增强 / 缺陷修复）。

2. 从 `main` 分支创建新的功能分支，分支命名规则为 `feature/资源分类-简述` 或 `fix/问题简述`，例如 `feature/football-add-asia-sources`。

3. 按照 `docs/developer/` 中的资源提交规范，更新 `data/sources.json` 或编写对应的检测插件代码，确保新增资源包含完整的名称、分类、URL、预期数据结构说明字段。

4. 在本地运行检测脚本 `python scripts/run_detector.py --check-new` 验证新增资源的可用性，并确保所有现有测试用例通过（`pytest tests/`）。

5. 提交 Pull Request 到 `main` 分支，PR 描述中必须关联对应的议题编号，并附上本地验证结果截图或日志摘要。

6. 等待项目维护者审核，审核周期不超过 5 个工作日。如有修改意见，请在反馈后 3 个工作日内完成更新并重新提交。

## 常见问题

**Q: 收录的资源出现访问失败或数据结构变更时，项目会自动处理吗？**

A: 项目后台内置的周期性检测任务每 6 小时会对所有资源执行一次 HTTP 请求测试，记录状态码、响应时间和响应体前 1024 字节的哈希值。当状态码变化（如 200 变为 404）或响应体哈希变动超过阈值时，系统会生成差异报告并通过配置的通知渠道发送。但项目不会自动修改资源条目，需要人工根据差异报告判断是否需要更新域名或调整解析模板。

**Q: 我可以将本项目用于商业产品中吗？**

A: 本项目代码本身采用 MIT 许可证，允许自由使用、修改、分发和商业应用。但请注意，本项目中收录的第三方资源链接各自拥有独立的服务条款和数据使用限制，商业使用前请务必查阅目标资源的官方政策。本项目仅提供导航与可用性参考，不授予任何第三方数据的额外使用权限。

**Q: 如何添加不在预设列表中的私有数据源？**

A: 您可以通过 Web 界面上的「新增资源」表单手动录入私有数据源的名称、域名和分类标签，系统会将其保存到本地数据库中并纳入后续检测队列。私有数据源仅对您自己的部署实例可见，不会自动提交到公共资源库。如需将其贡献给社区，请按照贡献指南中的流程提交 Pull Request。

## 许可证

MIT License。详见项目根目录下的 `LICENSE` 文件。

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:16
